import 'package:flutter/widgets.dart';

final class SidebarViewModel {
  // final AuthRepository _authRepository;
  // ValueNotifier<UserModel?> user;

  // SidebarViewModel({required AuthRepository authRepository}) : _authRepository = authRepository, user = authRepository.user {
  //   logout = Command0(_logout);
  // }
  
  // late final Command0<void> logout;
  // Future<Result<void>> _logout() async => await _authRepository.logout();
}
